/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["thebsgroup.in", "framerusercontent.com", "images.unsplash.com", "images.pexels.com"],
    },
};

export default nextConfig;
